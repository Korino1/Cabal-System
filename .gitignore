# Vendor docs / local extracts (do not commit copyrighted materials)
arch/zen4/57647_zen4_sog.txt
arch/**/*.pdf
arch/**/*.zip

# Agner Fog reference set (explicitly tracked in this repo)
!arch/main/
!arch/main/*.pdf
!arch/main/*.zip

# Local bundles / archives
*.7z

# Local runtime state (do not commit)
.cabal_runtime/

# Local build artifacts
cabal-mcp-runtime/target/
cabal-mcp-runtime/target_*/
cabal-mcp-runtime/target*/
